CREATE OR REPLACE MASKING POLICY CUSTOMER_ID_MASK

AS (VAL STRING)

RETURNS STRING ->

CASE

    WHEN CURRENT_ROLE() IN
        ('DATA_ENGINEER_ROLE')

    THEN VAL

    ELSE '********'

END;

ALTER TABLE MART.DIM_CUSTOMERS

MODIFY COLUMN CUSTOMER_ID

SET MASKING POLICY CUSTOMER_ID_MASK;